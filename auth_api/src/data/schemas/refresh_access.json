{
  "$schema": "http://json-schema.org/schema#",
  "title": "JSON schema for refresh token",
  "type": "object",
  "properties": {
    "access_token": {"type": "string"},
    "refresh_token": {"type": "string"}
  },
  "required": ["access_token", "access_token"]
}